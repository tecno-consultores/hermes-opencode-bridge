[pytest]
markers =
    integration: Pruebas de integración que requieren dependencias externas o red (ej. Hermes).
    unit: Pruebas unitarias completamente aisladas (mockeadas).
testpaths = tests
asyncio_default_fixture_loop_scope = function
pythonpath = .
